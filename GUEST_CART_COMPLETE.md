# ✅ Гостевая корзина реализована!

## 🎉 Что сделано

Добавлена возможность добавлять товары в корзину **без авторизации**. Гостевая корзина сохраняется в localStorage браузера и автоматически синхронизируется с серверной корзиной при входе.

---

## 📋 Изменения

### 1. ✅ Обновлен cartStore

**Файл:** `frontend/src/store/cartStore.ts`

**Новый функционал:**
- Гостевая корзина в localStorage
- Автоматическое определение: авторизован пользователь или нет
- Синхронизация гостевой корзины с серверной при входе

**Утилиты:**
```typescript
getGuestCart()    // Получить гостевую корзину из localStorage
saveGuestCart()   // Сохранить гостевую корзину
clearGuestCart()  // Очистить гостевую корзину
```

**Новые методы:**
```typescript
syncGuestCart()   // Синхронизация при входе
```

**Логика работы:**
- Если `cart !== null` → пользователь авторизован → работаем с API
- Если `cart === null` → гость → работаем с localStorage

---

### 2. ✅ Обновлены компоненты

**ProductCard.tsx:**
- Убрана проверка авторизации
- Передается объект product при добавлении в корзину
- Товар добавляется в localStorage для гостей

**ProductPage.tsx:**
- Убрана проверка авторизации
- Убрано сообщение "Войдите для добавления в корзину"
- Кнопка всегда активна

**Header.tsx:**
- Добавлена синхронизация гостевой корзины при входе
- Счетчик товаров работает для гостей и авторизованных

---

## 🔄 Как это работает

### Для гостей (не авторизованных):

1. **Добавление товара:**
   ```typescript
   addToCart(productId, quantity, product)
   // Сохраняется в localStorage: { items: [{product_id, quantity, product}] }
   ```

2. **Просмотр корзины:**
   - Счетчик в Header показывает количество товаров
   - Страница /cart показывает товары из localStorage

3. **Изменение количества:**
   ```typescript
   updateQuantity(productId, newQuantity)
   // Обновляется в localStorage
   ```

4. **Удаление товара:**
   ```typescript
   removeItem(productId)
   // Удаляется из localStorage
   ```

### При входе в систему:

1. **Автоматическая синхронизация:**
   ```typescript
   syncGuestCart()
   // 1. Получаем товары из localStorage
   // 2. Добавляем их в серверную корзину через API
   // 3. Очищаем localStorage
   // 4. Загружаем обновленную корзину с сервера
   ```

2. **Результат:**
   - Все товары из гостевой корзины переносятся в серверную
   - localStorage очищается
   - Пользователь видит все свои товары

---

## 💾 Структура данных

### localStorage:
```json
{
  "guest_cart": {
    "items": [
      {
        "product_id": "uuid-товара",
        "quantity": 2,
        "product": {
          "id": "uuid-товара",
          "name": "Яблоки",
          "price": 150,
          "image": "url",
          ...
        }
      }
    ]
  }
}
```

### Серверная корзина (API):
```json
{
  "items": [
    {
      "id": "uuid-элемента-корзины",
      "product_id": "uuid-товара",
      "quantity": 2,
      "product": { ... }
    }
  ],
  "total": 300,
  "items_count": 2
}
```

---

## 🎯 Примеры использования

### Добавление в корзину (работает для всех):
```typescript
const { addToCart } = useCartStore()

// Гость или авторизованный - не важно
await addToCart(product.id, 1, product)
showToast.success('Товар добавлен в корзину!')
```

### Получение количества товаров:
```typescript
const { getItemsCount } = useCartStore()
const count = getItemsCount()
// Для гостя: считает из localStorage
// Для авторизованного: считает из серверной корзины
```

### Получение суммы:
```typescript
const { getTotal } = useCartStore()
const total = getTotal()
// Для гостя: считает из localStorage (цена * количество)
// Для авторизованного: берет из серверной корзины
```

---

## ✅ Преимущества

1. **Лучший UX:**
   - Не нужно входить чтобы добавить товар
   - Можно собрать корзину и войти потом
   - Товары не теряются при закрытии браузера

2. **Автоматическая синхронизация:**
   - При входе все товары переносятся в серверную корзину
   - Не нужно добавлять заново

3. **Работает везде:**
   - ProductCard
   - ProductPage
   - Любой компонент может добавлять в корзину

4. **Безопасность:**
   - Гостевая корзина только в браузере
   - При входе данные переносятся на сервер
   - localStorage очищается после синхронизации

---

## 🧪 Тестирование

### Сценарий 1: Гость добавляет товары
1. Откройте сайт (не входите)
2. Добавьте несколько товаров в корзину
3. Проверьте счетчик в Header (должен показывать количество)
4. Откройте /cart (должны быть товары)
5. Закройте браузер и откройте снова
6. Товары должны остаться в корзине

### Сценарий 2: Синхронизация при входе
1. Добавьте товары как гость (не входя)
2. Войдите в систему (SMS авторизация)
3. Товары должны автоматически перенестись в серверную корзину
4. localStorage должен очиститься
5. Счетчик должен показывать правильное количество

### Сценарий 3: Авторизованный пользователь
1. Войдите в систему
2. Добавьте товары в корзину
3. Товары сохраняются на сервере
4. Выйдите и войдите снова
5. Товары должны остаться (загружаются с сервера)

---

## 📝 Технические детали

**localStorage ключ:** `guest_cart`

**Проверка авторизации:**
```typescript
const { cart } = get()
if (cart !== null) {
  // Авторизован - работаем с API
} else {
  // Гость - работаем с localStorage
}
```

**Синхронизация:**
- Вызывается в Header при монтировании
- Только если пользователь авторизован
- Только если есть товары в гостевой корзине

---

## 🎉 Готово к использованию!

Теперь пользователи могут:
- ✅ Добавлять товары без входа
- ✅ Собирать корзину как гости
- ✅ Войти позже и получить все товары
- ✅ Не терять корзину при закрытии браузера

**Обновите страницу (Ctrl+R) и попробуйте добавить товар без входа!** 🚀
