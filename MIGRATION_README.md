# 📦 Миграция данных из старой базы enb

## 🎯 Цель
Перенести данные из старой MySQL/MariaDB базы `enb` в новую PostgreSQL базу `edu_na_bazar`.

---

## 📋 Что у вас есть

1. ✅ Старая база данных `enb` (дамп в файле `enb_export.sql`)
2. ✅ MariaDB 12.1 установлена
3. ✅ Новая PostgreSQL база `edu_na_bazar` готова
4. ✅ Скрипты миграции готовы

---

## 🚀 Пошаговая инструкция

### Шаг 1: Проверьте MariaDB

```bash
# Запустите тест подключения
test_mariadb_connection.bat
```

**Что проверяется:**
- ✅ MariaDB доступна
- ✅ Можете подключиться с паролем
- ✅ База `enb` существует (или нет)

**Если база `enb` НЕ найдена** → переходите к Шагу 2  
**Если база `enb` найдена** → переходите к Шагу 3

---

### Шаг 2: Восстановите базу enb (если её нет)

```bash
# Запустите скрипт восстановления
restore_old_database.bat
```

**Что делает скрипт:**
1. Создаёт базу `enb` в MariaDB
2. Восстанавливает данные из `enb_export.sql`
3. Показывает статистику

**Требуется:**
- Файл `enb_export.sql` в корне проекта
- Пароль root для MariaDB

---

### Шаг 3: Запустите миграцию

```bash
cd backend\scripts
python migrate_data.py
```

**Процесс миграции:**

```
========================================
👤 МИГРАЦИЯ ПОЛЬЗОВАТЕЛЕЙ
========================================
📊 Найдено 1234 активных пользователей
✅ Мигрировано: 1234
⏭️  Пропущено: 0
❌ Ошибок: 0

========================================
📁 МИГРАЦИЯ КАТЕГОРИЙ
========================================
📊 Найдено 45 категорий
✅ Мигрировано: 45
...
```

**Время выполнения:** 5-15 минут

---

## 📊 Таблицы для миграции

| Старая таблица | Новая таблица | Описание |
|---------------|---------------|----------|
| `user` | `config.users` | Пользователи |
| `categories` | `market.categories` | Категории товаров |
| `companies` | `market.store_owners` | Магазины/Компании |
| `advert` | `market.products` | Товары/Объявления |
| `file` | `market.product_images` | Изображения товаров |

---

## ⚙️ Настройки миграции

### Изменить пароль MariaDB

Отредактируйте `backend/scripts/migrate_data.py`:

```python
MYSQL_CONFIG = {
    'host': 'localhost',
    'port': 3306,
    'user': 'root',
    'password': 'ВАШ_ПАРОЛЬ',  # ← Измените здесь
    'database': 'enb',
    ...
}
```

### Изменить порт MariaDB

```python
MYSQL_CONFIG = {
    'port': 3307,  # ← Если MariaDB на другом порту
    ...
}
```

---

## 🔍 Проверка результатов

### В PostgreSQL:

```bash
# Подключитесь к PostgreSQL
psql -U postgres -d edu_na_bazar

# Проверьте данные
SELECT 'Users' as table_name, COUNT(*) FROM config.users
UNION ALL
SELECT 'Categories', COUNT(*) FROM market.categories
UNION ALL
SELECT 'Store Owners', COUNT(*) FROM market.store_owners
UNION ALL
SELECT 'Products', COUNT(*) FROM market.products
UNION ALL
SELECT 'Product Images', COUNT(*) FROM market.product_images;
```

### Через Backend API:

```bash
# Запустите backend
cd backend
uvicorn main:app --reload

# Откройте в браузере
http://localhost:8000/docs

# Попробуйте endpoints:
GET /api/products
GET /api/categories
GET /api/stores
```

---

## 🐛 Решение проблем

### ❌ "Access denied for user 'root'"

**Причина:** Неверный пароль MariaDB

**Решение:**
```bash
mysql -u root
# В консоли MariaDB:
ALTER USER 'root'@'localhost' IDENTIFIED BY 'root';
FLUSH PRIVILEGES;
```

---

### ❌ "Can't connect to MySQL server"

**Причина:** MariaDB не запущена

**Решение:**
```bash
# Запустите MariaDB
net start MariaDB

# Проверьте статус
sc query MariaDB
```

---

### ❌ "Database 'enb' doesn't exist"

**Причина:** База не восстановлена

**Решение:**
```bash
restore_old_database.bat
```

---

### ❌ "ModuleNotFoundError: No module named 'pymysql'"

**Причина:** Не установлена библиотека

**Решение:**
```bash
cd backend
pip install pymysql
```

---

### ❌ "Duplicate entry for key 'phone'"

**Причина:** Пользователь с таким телефоном уже существует

**Решение:** Скрипт автоматически пропускает дубликаты. Это нормально.

---

## 📝 Важные замечания

### 1. Телефоны
- Нормализуются к формату `+7XXXXXXXXXX`
- Если телефона нет, генерируется фейковый: `+79000000XXX`

### 2. ID
- Новые ID будут отличаться от старых (автоинкремент PostgreSQL)
- Связи между таблицами сохраняются

### 3. Статусы
- **Пользователи:** Только активные (status = 10)
- **Компании:** Активные → status = 'approved'
- **Товары:** Активные → in_stock = true

### 4. Изображения
- Мигрируются только URL/пути
- Физические файлы нужно скопировать отдельно в `backend/uploads/`

### 5. Безопасность
- Старая база не изменяется
- Можно запускать миграцию несколько раз
- Дубликаты пропускаются автоматически

---

## ✅ Чек-лист миграции

- [ ] MariaDB запущена
- [ ] База `enb` восстановлена из дампа
- [ ] PostgreSQL запущена (Docker)
- [ ] База `edu_na_bazar` создана
- [ ] Установлен `pymysql`: `pip install pymysql`
- [ ] Настроен пароль в `migrate_data.py`
- [ ] Запущена миграция: `python migrate_data.py`
- [ ] Проверены данные в PostgreSQL
- [ ] Backend запущен и работает с новыми данными

---

## 🎯 Итого

**Вам НЕ нужен MySQL 5.7!**  
Используйте текущую **MariaDB 12.1** - она полностью совместима.

**Три команды для миграции:**
```bash
1. test_mariadb_connection.bat      # Проверка
2. restore_old_database.bat         # Восстановление (если нужно)
3. cd backend\scripts && python migrate_data.py  # Миграция
```

**Время:** 10-20 минут на всё.

---

## 📚 Дополнительные материалы

- `MIGRATION_ANALYSIS.md` - Детальный анализ таблиц
- `MIGRATION_QUICK_START.md` - Краткая инструкция
- `backend/scripts/migrate_data.py` - Скрипт миграции
- `backend/scripts/analyze_mysql_structure.py` - Анализ структуры старой БД
