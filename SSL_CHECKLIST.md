# ✅ SSL Checklist - Пошаговая проверка

## 📋 Перед началом

- [ ] Сервер развернут и работает (176.99.5.211)
- [ ] Docker контейнеры запущены
- [ ] Проект находится в `/opt/edu-na-bazar`
- [ ] Домен куплен (рекомендуется: edunabazar.ru)

---

## 🌐 Шаг 1: DNS настройка

### Добавить A-записи в панели управления доменом:

- [ ] A-запись: `@` → `176.99.5.211` (TTL: 3600)
- [ ] A-запись: `www` → `176.99.5.211` (TTL: 3600)

### Проверка DNS (выполнить локально):

```bash
nslookup edunabazar.ru
nslookup www.edunabazar.ru
```

- [ ] Оба домена возвращают IP: 176.99.5.211
- [ ] Подождал 5-60 минут для распространения DNS

---

## 🔐 Шаг 2: Получение SSL сертификатов

### Вариант A: Автоматический скрипт (рекомендуется)

**На локальной машине:**
```powershell
scp setup_ssl.sh root@176.99.5.211:/opt/edu-na-bazar/
```

**На сервере:**
```bash
cd /opt/edu-na-bazar
bash setup_ssl.sh
```

- [ ] Скрипт загружен на сервер
- [ ] Скрипт запущен
- [ ] Введен домен
- [ ] Введен email
- [ ] Сертификаты получены успешно

### Вариант B: Ручная установка

```bash
# Остановить контейнеры
docker-compose down

# Установить Certbot
apt update && apt install -y certbot python3-certbot-nginx

# Получить сертификаты
certbot certonly --standalone -d edunabazar.ru -d www.edunabazar.ru
```

- [ ] Контейнеры остановлены
- [ ] Certbot установлен
- [ ] Сертификаты получены
- [ ] Сертификаты находятся в `/etc/letsencrypt/live/edunabazar.ru/`

---

## 📝 Шаг 3: Конфигурация

### Создать nginx-ssl.conf:

- [ ] Файл `frontend/nginx-ssl.conf` создан
- [ ] Домен заменен на реальный (DOMAIN → edunabazar.ru)
- [ ] Пути к сертификатам правильные

### Обновить .env:

```env
DOMAIN=edunabazar.ru
ALLOWED_ORIGINS=https://edunabazar.ru,https://www.edunabazar.ru
REACT_APP_API_URL=https://edunabazar.ru
ENVIRONMENT=production
```

- [ ] `.env` обновлен
- [ ] Все URLs изменены на HTTPS
- [ ] ALLOWED_ORIGINS содержит HTTPS домены

### Обновить docker-compose.yml:

- [ ] Добавлены volumes для SSL сертификатов
- [ ] Frontend использует Dockerfile.ssl
- [ ] Порты 80 и 443 открыты

---

## 🚀 Шаг 4: Запуск

```bash
# Пересобрать frontend
docker-compose build --no-cache frontend

# Запустить все контейнеры
docker-compose up -d

# Проверить статус
docker-compose ps

# Проверить логи
docker-compose logs -f frontend
```

- [ ] Frontend пересобран
- [ ] Все контейнеры запущены
- [ ] Все контейнеры в статусе "Up"
- [ ] Нет ошибок в логах

---

## ✅ Шаг 5: Проверка HTTPS

### Проверка в браузере:

- [ ] Открыл https://edunabazar.ru
- [ ] Зеленый замочек 🔒 в адресной строке
- [ ] Сертификат от Let's Encrypt
- [ ] Сертификат валиден (не истек)
- [ ] HTTP редиректит на HTTPS (http://edunabazar.ru → https://edunabazar.ru)

### Проверка через curl:

```bash
# HTTP редирект
curl -I http://edunabazar.ru
# Должен вернуть: 301 Moved Permanently

# HTTPS работает
curl -I https://edunabazar.ru
# Должен вернуть: 200 OK
```

- [ ] HTTP возвращает 301 редирект
- [ ] HTTPS возвращает 200 OK

### Проверка через SSL Labs:

Открыть: https://www.ssllabs.com/ssltest/analyze.html?d=edunabazar.ru

- [ ] Оценка: A или A+
- [ ] Все протоколы безопасны
- [ ] Нет критических предупреждений

---

## 🔄 Шаг 6: Автообновление сертификатов

### Настроить cron:

```bash
crontab -e
```

Добавить:
```
0 3 * * * certbot renew --quiet --post-hook "docker-compose -f /opt/edu-na-bazar/docker-compose.yml restart frontend" >> /var/log/certbot-renew.log 2>&1
```

- [ ] Cron задача добавлена
- [ ] Проверка каждый день в 3:00

### Тест обновления:

```bash
certbot renew --dry-run
```

- [ ] Тест прошел успешно
- [ ] Вывод: "Congratulations, all simulated renewals succeeded"

---

## 🔍 Шаг 7: Финальная проверка

### Функциональность:

- [ ] Главная страница загружается
- [ ] Каталог товаров работает
- [ ] Карта товаров работает
- [ ] API запросы работают (проверить в DevTools → Network)
- [ ] Изображения загружаются
- [ ] Аутентификация работает
- [ ] Корзина работает

### Безопасность:

- [ ] Все ресурсы загружаются по HTTPS (нет Mixed Content)
- [ ] Security headers присутствуют (проверить в DevTools → Network → Headers)
- [ ] HSTS header присутствует
- [ ] CSP header присутствует

### Производительность:

- [ ] Gzip сжатие работает (проверить в DevTools → Network → Size)
- [ ] Статические файлы кэшируются
- [ ] Время загрузки приемлемое (<3 секунд)

---

## 📊 Мониторинг

### Настроить мониторинг:

- [ ] UptimeRobot настроен (https://uptimerobot.com)
- [ ] Telegram бот для уведомлений настроен
- [ ] Email уведомления от Let's Encrypt работают

### Логи:

```bash
# Логи Nginx
docker-compose logs -f frontend

# Логи Backend
docker-compose logs -f backend

# Логи Certbot
tail -f /var/log/certbot-renew.log
```

- [ ] Логи доступны
- [ ] Нет критических ошибок

---

## 🎉 Итоговая проверка

### Все работает:

- [x] ✅ DNS настроен
- [x] ✅ SSL сертификаты получены
- [x] ✅ HTTPS работает
- [x] ✅ HTTP редиректит на HTTPS
- [x] ✅ Оценка SSL Labs: A/A+
- [x] ✅ Автообновление настроено
- [x] ✅ Сайт полностью функционален
- [x] ✅ Мониторинг настроен

---

## 🐛 Troubleshooting

### Проблема: DNS не работает

```bash
# Проверить DNS
dig edunabazar.ru
nslookup edunabazar.ru

# Очистить DNS кэш (Windows)
ipconfig /flushdns

# Очистить DNS кэш (Linux)
systemd-resolve --flush-caches
```

### Проблема: Certbot не может получить сертификат

```bash
# Проверить что порт 80 свободен
netstat -tulpn | grep :80

# Остановить все контейнеры
docker-compose down

# Попробовать снова
certbot certonly --standalone -d edunabazar.ru -d www.edunabazar.ru
```

### Проблема: Mixed Content (HTTP ресурсы на HTTPS)

```bash
# Проверить .env
cat .env | grep URL

# Убедиться что все URLs с HTTPS
# Пересобрать frontend
docker-compose build --no-cache frontend
docker-compose up -d frontend
```

### Проблема: Nginx не запускается

```bash
# Проверить логи
docker-compose logs frontend

# Проверить конфигурацию
docker-compose exec frontend nginx -t

# Проверить сертификаты
ls -la /etc/letsencrypt/live/edunabazar.ru/
```

---

## 📞 Поддержка

Если что-то не работает:

1. **Проверьте документацию:**
   - [SSL_SETUP.md](SSL_SETUP.md) - полная инструкция
   - [SSL_QUICK_START.md](SSL_QUICK_START.md) - быстрый старт

2. **Проверьте логи:**
   ```bash
   docker-compose logs -f
   ```

3. **Проверьте статус:**
   ```bash
   docker-compose ps
   systemctl status docker
   ```

4. **Перезапустите:**
   ```bash
   docker-compose restart
   ```

---

## 🎯 Следующие шаги

После успешной настройки SSL:

- [ ] Настроить backup БД
- [ ] Настроить мониторинг производительности
- [ ] Добавить rate limiting
- [ ] Настроить CDN (Cloudflare)
- [ ] Добавить WAF (Web Application Firewall)

---

**Поздравляем! Ваш сайт защищен SSL и готов к production! 🔒🎉**
