# 🚀 Настройка Git и GitHub для проекта

## 📋 Шаг 1: Установка Git

### Windows:
1. Скачайте Git: https://git-scm.com/download/win
2. Запустите установщик
3. Выберите настройки по умолчанию
4. Перезапустите PowerShell/Terminal

### Проверка установки:
```bash
git --version
```

---

## 🔧 Шаг 2: Настройка Git

### Настройте имя и email:
```bash
git config --global user.name "Ваше Имя"
git config --global user.email "your.email@example.com"
```

### Проверка:
```bash
git config --global --list
```

---

## 📦 Шаг 3: Инициализация репозитория

### В корне проекта:
```bash
cd c:\python\edu-na-bazar
git init
```

### Добавьте все файлы:
```bash
git add .
```

### Первый коммит:
```bash
git commit -m "🎉 Initial commit: Еду на базар - маркетплейс прямых продаж"
```

---

## 🌐 Шаг 4: Создание репозитория на GitHub

### Вариант 1: Через веб-интерфейс
1. Откройте https://github.com
2. Войдите в аккаунт (или создайте новый)
3. Нажмите "+" → "New repository"
4. Заполните:
   - **Repository name:** `edu-na-bazar`
   - **Description:** `🛒 Маркетплейс прямых продаж от фермеров без посредников`
   - **Visibility:** Public или Private
   - **НЕ** добавляйте README, .gitignore, license (у нас уже есть)
5. Нажмите "Create repository"

### Вариант 2: Через GitHub CLI
```bash
# Установите GitHub CLI: https://cli.github.com/
gh auth login
gh repo create edu-na-bazar --public --source=. --remote=origin
```

---

## 🔗 Шаг 5: Подключение к GitHub

### Добавьте remote:
```bash
git remote add origin https://github.com/ВАШ_USERNAME/edu-na-bazar.git
```

### Или через SSH (рекомендуется):
```bash
git remote add origin git@github.com:ВАШ_USERNAME/edu-na-bazar.git
```

### Проверка:
```bash
git remote -v
```

---

## 📤 Шаг 6: Первый push

### Push в main ветку:
```bash
git branch -M main
git push -u origin main
```

### Если ошибка аутентификации:
```bash
# Используйте Personal Access Token
# Создайте токен: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
# Выберите scopes: repo (все)
# Используйте токен вместо пароля при push
```

---

## 🔄 Шаг 7: Ежедневная работа

### Проверить изменения:
```bash
git status
```

### Добавить файлы:
```bash
git add .                    # Все файлы
git add backend/main.py      # Конкретный файл
git add frontend/src/        # Папка
```

### Коммит:
```bash
git commit -m "✨ Добавлена регистрация с SMS"
git commit -m "🐛 Исправлена ошибка в корзине"
git commit -m "📝 Обновлена документация"
```

### Push:
```bash
git push
```

### Pull (получить изменения):
```bash
git pull
```

---

## 🎨 Шаг 8: Emoji для коммитов (опционально)

Используйте эмодзи для красивых коммитов:

- ✨ `:sparkles:` - Новая функция
- 🐛 `:bug:` - Исправление бага
- 📝 `:memo:` - Документация
- 🎨 `:art:` - Улучшение структуры кода
- ⚡ `:zap:` - Улучшение производительности
- 🔥 `:fire:` - Удаление кода/файлов
- 🚀 `:rocket:` - Деплой
- 💄 `:lipstick:` - UI/стили
- 🔧 `:wrench:` - Конфигурация
- 🌐 `:globe_with_meridians:` - Интернационализация
- 📦 `:package:` - Зависимости
- 🔒 `:lock:` - Безопасность

### Пример:
```bash
git commit -m "✨ Добавлена интеграция SMSC.RU для SMS"
git commit -m "🐛 Исправлена ошибка с гостевой корзиной"
git commit -m "💄 Обновлен дизайн кнопок"
```

---

## 📊 Шаг 9: Структура коммитов

### Хорошие коммиты:
```bash
✅ "✨ Добавлена страница О нас"
✅ "🐛 Исправлена ошибка с фильтрами категорий"
✅ "📝 Обновлена документация по API"
✅ "🎨 Рефакторинг компонента AuthModal"
```

### Плохие коммиты:
```bash
❌ "fix"
❌ "update"
❌ "asdfasdf"
❌ "работает"
```

---

## 🌿 Шаг 10: Работа с ветками (опционально)

### Создать новую ветку:
```bash
git checkout -b feature/payment-integration
git checkout -b bugfix/cart-error
```

### Переключиться на ветку:
```bash
git checkout main
git checkout feature/payment-integration
```

### Слить ветку:
```bash
git checkout main
git merge feature/payment-integration
```

### Удалить ветку:
```bash
git branch -d feature/payment-integration
```

---

## 🔐 Шаг 11: SSH ключи (рекомендуется)

### Генерация SSH ключа:
```bash
ssh-keygen -t ed25519 -C "your.email@example.com"
# Нажмите Enter 3 раза (без пароля)
```

### Копирование ключа:
```bash
# Windows PowerShell:
Get-Content ~/.ssh/id_ed25519.pub | Set-Clipboard

# Или вручную:
cat ~/.ssh/id_ed25519.pub
```

### Добавление на GitHub:
1. GitHub → Settings → SSH and GPG keys
2. New SSH key
3. Вставьте скопированный ключ
4. Save

### Проверка:
```bash
ssh -T git@github.com
```

---

## 📦 Что уже в .gitignore:

```
✅ __pycache__/
✅ venv/
✅ node_modules/
✅ .env
✅ .vscode/
✅ .idea/
✅ *.log
✅ .DS_Store
```

---

## 🚨 Важно!

### НЕ коммитьте:
- ❌ Пароли и API ключи
- ❌ .env файлы с секретами
- ❌ node_modules/
- ❌ venv/
- ❌ __pycache__/
- ❌ Большие бинарные файлы

### Если случайно закоммитили секреты:
```bash
# Удалите файл из истории
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch backend/.env" \
  --prune-empty --tag-name-filter cat -- --all

# Или используйте BFG Repo-Cleaner
```

---

## 📚 Полезные команды

```bash
# Отменить последний коммит (сохранить изменения)
git reset --soft HEAD~1

# Отменить изменения в файле
git checkout -- filename.py

# Посмотреть историю
git log --oneline --graph --all

# Посмотреть изменения
git diff

# Посмотреть изменения в файле
git diff backend/main.py

# Создать тег (версия)
git tag v1.0.0
git push origin v1.0.0
```

---

## 🎯 Быстрый старт (копируй и вставляй)

```bash
# 1. Инициализация
cd c:\python\edu-na-bazar
git init
git add .
git commit -m "🎉 Initial commit: Еду на базар"

# 2. Подключение к GitHub (замените USERNAME)
git remote add origin https://github.com/USERNAME/edu-na-bazar.git
git branch -M main
git push -u origin main

# 3. Ежедневная работа
git add .
git commit -m "✨ Описание изменений"
git push
```

---

## 🔗 Полезные ссылки

- **Git документация:** https://git-scm.com/doc
- **GitHub Guides:** https://guides.github.com/
- **Git Cheat Sheet:** https://education.github.com/git-cheat-sheet-education.pdf
- **Emoji Cheat Sheet:** https://gitmoji.dev/

---

## ✅ Готово!

После настройки ваш проект будет на GitHub и вы сможете:
- 📦 Делиться кодом
- 🔄 Работать из разных мест
- 📊 Отслеживать историю изменений
- 👥 Работать в команде
- 🚀 Настроить CI/CD

**Следующий шаг:** Установите Git и выполните команды из "Быстрый старт"! 🚀
