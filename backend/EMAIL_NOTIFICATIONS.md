# 📧 Email-уведомления для заказов

## Обзор

Система автоматических email-уведомлений для покупателей и магазинов при изменении статусов заказов.

## Технологии

- **FastAPI-Mail** - отправка email через SMTP
- **Jinja2** - шаблонизатор для HTML писем
- **Asyncio** - асинхронная отправка без блокировки API

## Настройка

### 1. Установка зависимостей

```bash
pip install fastapi-mail==1.4.1 jinja2==3.1.2
```

### 2. Переменные окружения (.env)

```env
# Email Settings
MAIL_ENABLED=true  # false для разработки (письма не отправляются)
MAIL_USERNAME=noreply@edu-na-bazar.ru
MAIL_PASSWORD=your_password_here
MAIL_FROM=noreply@edu-na-bazar.ru
MAIL_FROM_NAME=Еду на базар
MAIL_PORT=587
MAIL_SERVER=smtp.yandex.ru
MAIL_STARTTLS=true
MAIL_SSL_TLS=false
MAIL_USE_CREDENTIALS=true
MAIL_VALIDATE_CERTS=true
```

### 3. Поддерживаемые SMTP серверы

**Yandex (по умолчанию):**
- Server: `smtp.yandex.ru`
- Port: `587`
- STARTTLS: `true`

**Gmail:**
- Server: `smtp.gmail.com`
- Port: `587`
- STARTTLS: `true`
- Требуется App Password (не основной пароль!)

**Mail.ru:**
- Server: `smtp.mail.ru`
- Port: `587`
- STARTTLS: `true`

## События и шаблоны

### Для покупателей:

| Событие | Шаблон | Когда отправляется |
|---------|--------|-------------------|
| Заказ создан | `order_created.html` | При создании заказа |
| Заказ подтвержден | `order_confirmed.html` | Статус → `paid` |
| Заказ готов | `order_ready.html` | Статус → `delivering` |
| Заказ завершен | `order_completed.html` | Статус → `completed` |
| Заказ отменен | `order_cancelled.html` | Статус → `cancelled` |

### Для магазинов:

| Событие | Шаблон | Когда отправляется |
|---------|--------|-------------------|
| Новый заказ | `new_order_store.html` | При создании заказа |

## Структура файлов

```
backend/
├── services/
│   └── email_service.py          # Сервис отправки email
├── templates/
│   └── email/
│       ├── base.html             # Базовый шаблон
│       ├── order_created.html    # Заказ создан
│       ├── order_confirmed.html  # Заказ подтвержден
│       ├── order_ready.html      # Заказ готов
│       ├── order_completed.html  # Заказ завершен
│       ├── order_cancelled.html  # Заказ отменен
│       └── new_order_store.html  # Новый заказ (магазину)
└── api/routers/
    └── orders.py                 # Триггеры отправки
```

## Использование

### В коде (автоматически)

Email отправляются автоматически при:
- Создании заказа (`POST /api/orders/`)
- Изменении статуса (`PUT /api/orders/{id}/status`)

### Ручная отправка

```python
from services.email_service import email_service

# Отправка уведомления о создании заказа
await email_service.send_order_created(
    email_to="customer@example.com",
    order_data={
        'order_id': 12345,
        'customer_name': 'Иван Иванов',
        'store_name': 'Фермерские продукты',
        'created_at': '29.11.2025 22:00',
        'delivery_address': 'Москва, ул. Ленина, 1',
        'items': [
            {'name': 'Молоко', 'quantity': 2, 'unit': 'л', 'price': 80},
            {'name': 'Хлеб', 'quantity': 1, 'unit': 'шт', 'price': 50}
        ],
        'total_amount': 210
    }
)
```

## Режим разработки

При `MAIL_ENABLED=false` письма **не отправляются**, но логируются в консоль:

```
📧 [DEV MODE] Email не отправлен (MAIL_ENABLED=false): user@example.com - Заказ #12345 оформлен
   Шаблон: order_created, Данные: {...}
```

Это позволяет тестировать без реальной отправки писем.

## Дизайн писем

### Особенности:

- ✅ **Адаптивный дизайн** - корректно отображается на всех устройствах
- ✅ **Градиентный header** - фирменный стиль
- ✅ **Цветные статусы** - наглядное отображение состояния заказа
- ✅ **Кнопки действий** - прямые ссылки на заказ/каталог
- ✅ **Читаемая типографика** - системные шрифты
- ✅ **Структурированная информация** - таблицы с данными заказа

### Цветовая схема статусов:

- 🔵 **Создан** (`created`) - синий
- 🟠 **Подтвержден** (`paid`) - оранжевый
- 🟢 **Готов** (`delivering`) - зеленый
- 🟣 **Завершен** (`completed`) - фиолетовый
- 🔴 **Отменен** (`cancelled`) - красный

## Безопасность

### Рекомендации:

1. **Не храните пароли в коде** - используйте `.env`
2. **Используйте App Passwords** для Gmail
3. **Включите 2FA** на почтовом аккаунте
4. **Ограничьте SMTP доступ** по IP (если возможно)
5. **Логируйте отправку** для аудита

### Защита от спама:

- ✅ Письма отправляются только при реальных событиях
- ✅ Один тип письма на одно событие
- ✅ Нет рекламных рассылок
- ✅ Ссылка "отписаться" не нужна (транзакционные письма)

## Мониторинг

### Логи отправки:

```python
✅ Email отправлен: user@example.com - Заказ #12345 оформлен
❌ Ошибка отправки email: [Errno 111] Connection refused
```

### Проверка статуса:

```bash
# Проверить подключение к SMTP
telnet smtp.yandex.ru 587

# Проверить логи
docker-compose logs backend | grep "Email"
```

## Troubleshooting

### Письма не отправляются

1. Проверьте `MAIL_ENABLED=true` в `.env`
2. Проверьте логин/пароль SMTP
3. Проверьте порт и сервер
4. Для Gmail используйте App Password
5. Проверьте firewall/антивирус

### Письма попадают в спам

1. Настройте SPF/DKIM/DMARC записи
2. Используйте корпоративный домен
3. Прогрейте IP адрес отправителя
4. Не используйте спам-слова в теме

### Медленная отправка

- Используется `asyncio.create_task()` - отправка не блокирует API
- Если нужно - настройте очередь (Celery/RQ)

## Расширение

### Добавление нового типа письма:

1. Создайте шаблон в `templates/email/`
2. Добавьте метод в `EmailService`
3. Вызовите в нужном роутере

Пример:

```python
# services/email_service.py
@staticmethod
async def send_product_review_request(email_to: EmailStr, data: Dict):
    return await EmailService.send_email(
        email_to=email_to,
        subject=f"Оцените товар из заказа #{data['order_id']}",
        template_name="review_request",
        template_data=data
    )
```

## Лицензия

MIT License - используйте свободно в своих проектах! 🚀
