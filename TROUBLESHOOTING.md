# 🔧 Решение проблем

## ❌ "Мои заказы" показывает 401 Unauthorized

### Причина:
После исправления сохранения токена в localStorage, старые сессии не имеют токена в правильном месте.

### Решение:

**Вариант 1: Перелогиниться**
1. Откройте сайт
2. Нажмите на аватар пользователя (👤)
3. Нажмите "Выйти"
4. Войдите заново через SMS
5. Теперь "Мои заказы" будут работать

**Вариант 2: Очистить localStorage вручную**
1. Откройте DevTools (F12)
2. Перейдите в Application → Local Storage → http://localhost:3000
3. Удалите все ключи
4. Обновите страницу (F5)
5. Войдите заново

**Вариант 3: Выполнить в консоли**
```javascript
// Откройте DevTools (F12) → Console
localStorage.clear()
location.reload()
```

---

## ❌ Backend не отвечает / 500 ошибки

### Причина:
Изменения в моделях БД не синхронизированы с реальной структурой таблиц.

### Решение:
1. Убедитесь что вы на ветке `main`:
```bash
git checkout main
```

2. Перезапустите backend:
```bash
cd backend
# Остановите текущий процесс (Ctrl+C)
python main.py
```

---

## ❌ Товары не загружаются / ошибка "column does not exist"

### Причина:
Модели содержат поля, которых нет в БД (например, после переключения веток).

### Решение:
1. Переключитесь на стабильную ветку:
```bash
git checkout main
```

2. Перезапустите backend:
```bash
cd backend
python main.py
```

---

## ❌ Frontend не обновляется после изменений

### Решение:
1. Остановите dev server (Ctrl+C)
2. Очистите кэш:
```bash
cd frontend
rm -rf node_modules/.cache
```
3. Запустите заново:
```bash
npm start
```

---

## ❌ Ошибки CORS

### Решение:
Убедитесь что backend запущен на `http://localhost:8000` и frontend на `http://localhost:3000`.

Проверьте в `backend/main.py`:
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:3000"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

---

## 🔍 Как проверить что все работает

### Backend:
```bash
curl http://localhost:8000/health
# Должен вернуть: {"status":"ok"}
```

### Авторизация:
```bash
curl http://localhost:8000/api/auth/send-sms -X POST -H "Content-Type: application/json" -d '{"phone":"+79991234567"}'
# Должен вернуть код 200 и SMS отправлен
```

### Товары:
```bash
curl http://localhost:8000/api/products/?limit=1
# Должен вернуть список товаров
```

---

## 📝 Логи

### Backend логи:
Смотрите в консоли где запущен `python main.py`

### Frontend логи:
1. Откройте DevTools (F12)
2. Перейдите в Console
3. Смотрите ошибки (красные сообщения)

### Network запросы:
1. Откройте DevTools (F12)
2. Перейдите в Network
3. Обновите страницу
4. Смотрите какие запросы падают с ошибками

---

## 🆘 Если ничего не помогло

1. **Остановите все процессы:**
```bash
# Остановите backend (Ctrl+C)
# Остановите frontend (Ctrl+C)
```

2. **Проверьте что вы на main:**
```bash
git status
git checkout main
```

3. **Перезапустите все:**
```bash
# Terminal 1 - Backend
cd backend
python main.py

# Terminal 2 - Frontend
cd frontend
npm start
```

4. **Очистите браузер:**
- Откройте DevTools (F12)
- Application → Clear storage → Clear site data
- Обновите страницу (F5)

5. **Перелогиньтесь:**
- Войдите заново через SMS
