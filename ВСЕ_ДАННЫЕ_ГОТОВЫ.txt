╔════════════════════════════════════════════════════════════════╗
║       ✅ ВСЕ ДАННЫЕ ИЗВЛЕЧЕНЫ ИЗ БАЗЫ ENB!                     ║
╚════════════════════════════════════════════════════════════════╝

📊 ПОЛНАЯ СТАТИСТИКА:

┌──────────────┬────────────────────────────┬──────────────┐
│ Таблица      │ SQL файл                   │ Записей      │
├──────────────┼────────────────────────────┼──────────────┤
│ categories   │ categories_inserts.sql     │ 333          │
│ companies    │ companies_inserts.sql      │ 689          │
│ user         │ user_inserts.sql           │ 2,582        │
│ advert       │ advert_inserts.sql         │ 15,799       │
│ review       │ review_inserts.sql         │ 19           │
│ file         │ file_inserts.sql           │ 9,765        │
├──────────────┴────────────────────────────┴──────────────┤
│ ИТОГО:                                     29,187 записей│
└───────────────────────────────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 ИМПОРТ ВСЕХ ДАННЫХ В MySQL 5.7:

ШАГ 1: Запустите MySQL 5.7
───────────────────────────

C:\mysql57\bin\mysqld.exe --datadir=C:\mysql57_data_new

Дождитесь: "ready for connections"


ШАГ 2: Импортируйте ВСЕ данные (в новом окне)
──────────────────────────────────────────────

АВТОМАТИЧЕСКИ (рекомендуется):

    .\import_all_data.bat

Скрипт импортирует все 6 таблиц автоматически!


ИЛИ ВРУЧНУЮ (по одной таблице):

    C:\mysql57\bin\mysql.exe -u root < categories_inserts.sql
    C:\mysql57\bin\mysql.exe -u root < companies_inserts.sql
    C:\mysql57\bin\mysql.exe -u root < user_inserts.sql
    C:\mysql57\bin\mysql.exe -u root < advert_inserts.sql
    C:\mysql57\bin\mysql.exe -u root < review_inserts.sql
    C:\mysql57\bin\mysql.exe -u root < file_inserts.sql


ШАГ 3: Проверьте данные
────────────────────────

C:\mysql57\bin\mysql.exe -u root -e "USE enb; SHOW TABLES;"

Должно быть 6 таблиц:
  • categories
  • companies
  • user
  • advert
  • review
  • file

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📁 СОЗДАННЫЕ ФАЙЛЫ:

СКРИПТЫ ИЗВЛЕЧЕНИЯ:
  ✅ extract_categories.py
  ✅ extract_companies.py
  ✅ extract_users.py
  ✅ extract_adverts.py
  ✅ extract_reviews.py
  ✅ extract_files.py

SQL ФАЙЛЫ ДЛЯ ИМПОРТА:
  ✅ categories_inserts.sql    (333 записи)
  ✅ companies_inserts.sql     (689 записей)
  ✅ user_inserts.sql          (2,582 записи)
  ✅ advert_inserts.sql        (15,799 записей)
  ✅ review_inserts.sql        (19 записей)
  ✅ file_inserts.sql          (9,765 записей)

АВТОМАТИЗАЦИЯ:
  ✅ import_all_data.bat       (импорт всех таблиц)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⏭️ СЛЕДУЮЩИЙ ШАГ - МИГРАЦИЯ В POSTGRESQL:

После успешного импорта в MySQL 5.7:

    cd backend\scripts
    python migrate_data.py

Скрипт автоматически:
  • Подключится к MySQL (база enb)
  • Подключится к PostgreSQL (база edu_na_bazar)
  • Мигрирует данные с преобразованием структуры
  • Создаст связи между таблицами
  • Покажет статистику миграции

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ:

1. ПАРОЛИ ПОЛЬЗОВАТЕЛЕЙ
   • Пароли НЕ мигрируются из соображений безопасности
   • Пользователям нужно будет сбросить пароли
   • Используйте функцию "Забыли пароль?"

2. СВЯЗИ МЕЖДУ ТАБЛИЦАМИ
   • ID могут не совпадать с оригинальными
   • Связи восстановятся при миграции в PostgreSQL
   • Скрипт migrate_data.py создаст правильные связи

3. ФАЙЛЫ/ФОТО
   • Извлечены только пути к файлам
   • Сами файлы нужно скопировать отдельно
   • Путь к файлам: /uploads/

4. КАЧЕСТВО ДАННЫХ
   • Данные извлечены из .ibd файлов частично
   • Некоторые поля могут быть неточными
   • Рекомендуется проверить данные после импорта

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ ВСЁ ГОТОВО К ИМПОРТУ! 🎉

Время выполнения импорта: ~10 минут
Время миграции в PostgreSQL: ~15 минут

ОБЩЕЕ ВРЕМЯ: ~25 минут

НАЧИНАЙТЕ С ШАГА 1! 🚀
