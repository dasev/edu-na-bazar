# 📊 Отчёт о миграции данных в схему temp

## ✅ Статус миграции: ЗАВЕРШЕНА

Дата: 24 ноября 2025, 14:17

---

## 📋 Результаты по таблицам

### ✅ Успешно загружены:

| Таблица | Записей | Статус |
|---------|---------|--------|
| `temp.categories` | 349 | ✅ OK |
| `temp.companies` | 1,401 | ✅ OK |
| `temp.sub_categories` | 1,018 | ✅ OK |
| `temp.file` | 9,765 | ✅ OK |

**Итого успешно:** ~12,533 записей

---

### ⚠️ Частично загружены (с ошибками):

| Таблица | Попыток | Загружено | Ошибок | Причина |
|---------|---------|-----------|--------|---------|
| `temp.seller` | 20 | 0 | 14 | Ошибки транзакций |
| `temp.user` | 15,799 | 0 | 14 | Ошибки транзакций |
| `temp.review` | 19 | 0 | 14 | Ошибки транзакций |
| `temp.advert` | 15,799 | 0 | 14,624 | Незакрытые кавычки в SQL |

---

## 🔍 Анализ проблем

### Проблема 1: Незакрытые кавычки (unterminated quoted string)

**Файл:** `advert_inserts.sql`

**Причина:** В данных объявлений есть текст с незакрытыми кавычками:
```sql
INSERT INTO advert (...) VALUES (..., 'текст с незакрытой кавычкой...
```

**Решение:**
1. Экранировать кавычки в исходных данных
2. Использовать параметризованные запросы
3. Очистить данные перед импортом

### Проблема 2: Ошибки транзакций

**Причина:** После первой ошибки транзакция переходит в состояние "aborted", и все последующие команды игнорируются.

**Решение:** ✅ Исправлено в новой версии скрипта:
- Каждая запись коммитится отдельно
- При ошибке делается rollback
- Продолжается обработка следующих записей

---

## 🔧 Что делать дальше

### Вариант 1: Перезапустить миграцию с исправленным скриптом

```bash
# Удалить схему temp
psql -U postgres -d edu_na_bazar -c "DROP SCHEMA temp CASCADE;"

# Запустить миграцию заново
python backend\scripts\migrate_to_temp_schema.py
```

### Вариант 2: Очистить проблемные данные

Создать скрипт для очистки SQL файлов:

```python
# clean_sql_data.py
import re

def clean_sql_file(input_file, output_file):
    with open(input_file, 'r', encoding='utf-8') as f:
        content = f.read()
    
    # Экранируем одинарные кавычки
    # Заменяем ' на ''
    inserts = re.findall(r"INSERT INTO.*?;", content, re.DOTALL)
    
    cleaned_inserts = []
    for insert in inserts:
        # Экранируем кавычки внутри VALUES
        cleaned = insert.replace("\\'", "''")
        cleaned_inserts.append(cleaned)
    
    with open(output_file, 'w', encoding='utf-8') as f:
        f.write('\n'.join(cleaned_inserts))

# Очистить проблемные файлы
clean_sql_file('advert_inserts.sql', 'advert_inserts_clean.sql')
clean_sql_file('user_inserts.sql', 'user_inserts_clean.sql')
```

### Вариант 3: Использовать COPY вместо INSERT

PostgreSQL COPY команда более устойчива к ошибкам:

```python
# Конвертировать SQL в CSV
# Загрузить через COPY
COPY temp.advert FROM '/path/to/advert.csv' WITH CSV HEADER;
```

---

## 📊 Проверка загруженных данных

### SQL запросы для проверки:

```sql
-- Количество записей
SELECT 
    'categories' as table_name, COUNT(*) FROM temp.categories
UNION ALL
SELECT 'companies', COUNT(*) FROM temp.companies
UNION ALL
SELECT 'sub_categories', COUNT(*) FROM temp.sub_categories
UNION ALL
SELECT 'file', COUNT(*) FROM temp.file;

-- Примеры данных
SELECT * FROM temp.categories LIMIT 10;
SELECT * FROM temp.companies LIMIT 10;
SELECT * FROM temp.file LIMIT 10;
```

---

## ✅ Успешно загруженные данные

### 1. Категории (349 записей)

```sql
SELECT id, name, parent_id FROM temp.categories LIMIT 5;
```

Примеры:
- /agrotovary/
- /agroturizm/
- /arenda-spetstekhniki/
- /bakaleya/
- /frukty-yagody/

### 2. Компании (1,401 записей)

```sql
SELECT id, name, email, address FROM temp.companies LIMIT 5;
```

Данные включают:
- Название компании
- Email, телефон
- Адрес
- Статус

### 3. Подкатегории (1,018 записей)

```sql
SELECT id, name, category_id FROM temp.sub_categories LIMIT 5;
```

Примеры:
- Для комбайнов
- Для тракторов
- Для погрузчиков
- Резинотехнические изделия

### 4. Файлы (9,765 записей)

```sql
SELECT id, filename, filepath FROM temp.file LIMIT 5;
```

Данные о загруженных файлах (изображения товаров, документы).

---

## 🎯 Рекомендации

### Для продолжения миграции:

1. **Исправить скрипт миграции** ✅ СДЕЛАНО
   - Добавлен rollback при ошибках
   - Каждая запись коммитится отдельно

2. **Очистить проблемные SQL файлы**
   - Экранировать кавычки
   - Удалить невалидные символы
   - Проверить синтаксис

3. **Перезапустить миграцию**
   ```bash
   python backend\scripts\migrate_to_temp_schema.py
   ```

4. **Создать маппинг данных**
   - temp.categories → categories
   - temp.companies → stores
   - temp.file → product_images
   - temp.advert → products (после очистки)

---

## 📝 Следующие шаги

1. ✅ Схема `temp` создана
2. ✅ 4 таблицы успешно загружены (~12,533 записей)
3. ⏳ Исправить проблемные SQL файлы
4. ⏳ Перезапустить миграцию для оставшихся таблиц
5. ⏳ Создать скрипт маппинга данных
6. ⏳ Перенести данные в основные таблицы

---

## 🔗 Полезные ссылки

- [Скрипт миграции](backend/scripts/migrate_to_temp_schema.py)
- [Проверка данных](check_temp_data.sql)
- [Документация](TEMP_MIGRATION.md)

---

**Статус:** Миграция частично завершена. Требуется очистка данных для полной загрузки.
