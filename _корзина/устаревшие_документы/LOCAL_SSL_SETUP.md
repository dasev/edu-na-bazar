# 🔒 Настройка локального SSL для разработки

## 📋 Зачем нужно?

Если нужно тестировать HTTPS локально (например, для проверки SSL функций, cookies, или интеграций требующих HTTPS).

---

## ⚡ Быстрый старт

### Шаг 1: Сгенерировать self-signed сертификат

```bash
# Запустить скрипт генерации
generate_local_ssl.bat
```

Или вручную:

```bash
# Создать папку
mkdir ssl

# Сгенерировать сертификат
openssl req -x509 -nodes -days 365 -newkey rsa:2048 ^
  -keyout ssl/localhost.key ^
  -out ssl/localhost.crt ^
  -subj "/C=RU/ST=Moscow/L=Moscow/O=Dev/CN=localhost"
```

### Шаг 2: Остановить текущие контейнеры

```bash
docker-compose down
```

### Шаг 3: Запустить с SSL

```bash
docker-compose -f docker-compose.local-ssl.yml up -d
```

### Шаг 4: Открыть в браузере

```
✅ HTTP:  http://localhost       (редирект на HTTPS)
✅ HTTPS: https://localhost      (работает с self-signed)
```

---

## ⚠️ Предупреждение браузера

При первом открытии https://localhost браузер покажет предупреждение о небезопасном соединении:

**Chrome/Edge:**
1. Нажать "Advanced" (Дополнительно)
2. Нажать "Proceed to localhost (unsafe)" (Перейти на localhost)

**Firefox:**
1. Нажать "Advanced" (Дополнительно)
2. Нажать "Accept the Risk and Continue" (Принять риск)

Это нормально для self-signed сертификатов в локальной разработке!

---

## 🔄 Вернуться к HTTP (без SSL)

```bash
# Остановить SSL контейнеры
docker-compose -f docker-compose.local-ssl.yml down

# Запустить обычные
docker-compose up -d
```

---

## 📝 Что создано?

### Файлы:

- `ssl/localhost.key` - приватный ключ (не коммитить!)
- `ssl/localhost.crt` - сертификат (не коммитить!)
- `docker-compose.local-ssl.yml` - Docker конфигурация с SSL
- `frontend/Dockerfile.local-ssl` - Dockerfile с SSL поддержкой
- `frontend/nginx-local-ssl.conf` - Nginx конфигурация с SSL

### Конфигурация:

- **HTTP (порт 80)** → редирект на HTTPS
- **HTTPS (порт 443)** → работает с self-signed сертификатом
- **Health check** → `/health` доступен по HTTP

---

## 🚀 Production SSL

Для production используйте **Let's Encrypt** (бесплатно):

```bash
# На сервере с доменом
bash setup_ssl_prod.sh
```

Документация: `SSL_DEV_TO_PROD.md`

---

## 🐛 Troubleshooting

### Ошибка: "openssl не найден"

Установите OpenSSL:
- Скачать: https://slproweb.com/products/Win32OpenSSL.html
- Или через Chocolatey: `choco install openssl`

### Ошибка: "порт 443 занят"

Проверьте, что другие приложения не используют порт 443:
```bash
netstat -ano | findstr :443
```

### Сертификат не работает

Убедитесь что:
1. Файлы `ssl/localhost.key` и `ssl/localhost.crt` существуют
2. Контейнер запущен с правильным docker-compose файлом
3. Volume смонтирован: `docker inspect edu-na-bazar-frontend`

---

## 📚 Дополнительно

### Проверить SSL конфигурацию

```bash
# Внутри контейнера
docker exec edu-na-bazar-frontend nginx -t

# Проверить сертификат
openssl x509 -in ssl/localhost.crt -text -noout
```

### Логи Nginx

```bash
docker logs -f edu-na-bazar-frontend
```

---

**Создано:** 27.11.2025  
**Для проекта:** Еду на базар
