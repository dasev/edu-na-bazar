# ❌ Почему HTTPS не работает локально?

## 🔍 Проблема

```
Request URL: https://localhost/
Referrer Policy: strict-origin-when-cross-origin
❌ Не удается получить доступ к сайту
```

---

## 💡 Причина

**SSL сертификаты не настроены!**

Текущая конфигурация Docker использует только HTTP (порт 80), без SSL/HTTPS.

---

## ✅ Решение 1: Использовать HTTP (рекомендуется)

Для локальной разработки используйте **HTTP**:

```
✅ http://localhost       - работает
❌ https://localhost      - не работает
```

**Это нормально для локальной разработки!**

---

## 🔒 Решение 2: Настроить локальный SSL (опционально)

Если нужен HTTPS локально:

### Быстрая настройка:

```bash
# 1. Сгенерировать self-signed сертификат
generate_local_ssl.bat

# 2. Остановить текущие контейнеры
docker-compose down

# 3. Запустить с SSL
docker-compose -f docker-compose.local-ssl.yml up -d

# 4. Открыть https://localhost
# (браузер покажет предупреждение - это нормально для self-signed)
```

Подробная инструкция: **LOCAL_SSL_SETUP.md**

---

## 🚀 Production SSL

Для production сервера используйте **Let's Encrypt** (бесплатно):

```bash
# На сервере с доменом edunabazar.ru
ssh root@176.99.5.211
cd /opt/edu-na-bazar
bash setup_ssl_prod.sh
```

Документация: **SSL_DEV_TO_PROD.md**

---

## 📊 Сравнение вариантов

| Вариант | Использование | SSL | Сложность |
|---------|---------------|-----|-----------|
| **HTTP** | Локальная разработка | ❌ Нет | ⭐ Простой |
| **Self-signed SSL** | Локальное тестирование HTTPS | ⚠️ Да (с предупреждением) | ⭐⭐ Средний |
| **Let's Encrypt** | Production сервер | ✅ Да (валидный) | ⭐⭐⭐ Автоматический |

---

## 🎯 Рекомендация

**Для локальной разработки:** используйте HTTP (`http://localhost`)

**Для production:** настройте Let's Encrypt на сервере

---

## 📞 Быстрые команды

```bash
# Текущая конфигурация (HTTP only)
docker-compose up -d
# Доступ: http://localhost

# С локальным SSL
docker-compose -f docker-compose.local-ssl.yml up -d
# Доступ: https://localhost

# Проверить статус
docker ps

# Логи
docker logs -f edu-na-bazar-frontend
```

---

**Обновлено:** 27.11.2025
