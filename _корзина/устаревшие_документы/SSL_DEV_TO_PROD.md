# 🚀 SSL Setup: Development → Production

## 📋 Процесс в 2 этапа

### Этап 1: Подготовка на Development (локально)
### Этап 2: Финальная настройка на Production (сервер)

---

## 🖥️ ЭТАП 1: Development (Локально)

### ✅ Что уже сделано:

1. **Документация создана** (6 файлов):
   - SSL_SETUP.md
   - SSL_COMMERCIAL_SETUP.md
   - SSL_QUICK_START.md
   - SSL_CHECKLIST.md
   - SSL_SUMMARY.md
   - SSL_COMPARISON.md

2. **Скрипты созданы** (3 файла):
   - setup_ssl.sh (универсальный)
   - install_commercial_ssl.sh (для купленных)
   - **setup_ssl_prod.sh** (специально для edunabazar.ru)

3. **Docker конфигурации**:
   - docker-compose.ssl.yml
   - frontend/Dockerfile.ssl
   - frontend/nginx-ssl.conf.template

4. **.gitignore обновлен**:
   - Добавлены правила для SSL файлов
   - Сертификаты и ключи не будут коммититься

---

### 📦 Что нужно закоммитить и запушить:

```bash
# 1. Проверить статус
git status

# 2. Добавить новые файлы
git add .gitignore
git add SSL_*.md
git add setup_ssl.sh setup_ssl_prod.sh install_commercial_ssl.sh
git add docker-compose.ssl.yml
git add frontend/Dockerfile.ssl
git add frontend/nginx-ssl.conf.template
git add README.md

# 3. Проверить что НЕ добавляются секреты
git status
# Убедиться что нет:
# - .env
# - *.key
# - *.crt
# - *.pem

# 4. Коммит
git commit -m "feat: add SSL/HTTPS setup for edunabazar.ru

- Add comprehensive SSL documentation (6 files)
- Add automatic setup scripts (3 scripts)
  * setup_ssl.sh - universal script
  * setup_ssl_prod.sh - production script for edunabazar.ru
  * install_commercial_ssl.sh - for commercial certificates
- Add Docker SSL configurations
- Add Nginx SSL template
- Update .gitignore for SSL files
- Update README with SSL documentation links

Ready for production deployment on edunabazar.ru"

# 5. Пуш
git push origin main
```

---

## 🌐 Подготовка DNS (ВАЖНО!)

### Перед запуском на production:

**В панели управления доменом edunabazar.ru добавить A-записи:**

```
Тип    Имя    Значение           TTL
A      @      176.99.5.211       3600
A      www    176.99.5.211       3600
```

**Проверка DNS (выполнить локально):**
```bash
nslookup edunabazar.ru
nslookup www.edunabazar.ru

# Оба должны вернуть: 176.99.5.211
```

⏰ **Подождать 5-60 минут** пока DNS записи распространятся!

---

## 🖥️ ЭТАП 2: Production (Сервер 176.99.5.211)

### Шаг 1: Подключиться к серверу

```bash
ssh root@176.99.5.211
# Пароль: sIAS6APDsKh0bL
```

---

### Шаг 2: Обновить код из Git

```bash
cd /opt/edu-na-bazar

# Получить последние изменения
git pull origin main

# Проверить что скрипт загружен
ls -la setup_ssl_prod.sh

# Сделать исполняемым
chmod +x setup_ssl_prod.sh
```

---

### Шаг 3: Запустить production скрипт

```bash
# Запустить автоматическую настройку
bash setup_ssl_prod.sh
```

**Скрипт автоматически:**
1. ✅ Проверит предварительные условия
2. ✅ Проверит DNS записи
3. ✅ Остановит контейнеры
4. ✅ Установит Certbot
5. ✅ Получит SSL сертификаты для edunabazar.ru
6. ✅ Создаст nginx-ssl.conf с реальным доменом
7. ✅ Обновит .env (HTTPS URLs)
8. ✅ Обновит docker-compose.yml (volumes)
9. ✅ Пересоберет frontend
10. ✅ Запустит контейнеры
11. ✅ Настроит автообновление (cron)
12. ✅ Проверит результат

**Время выполнения: 5-10 минут**

---

### Шаг 4: Проверка результата

```bash
# Проверить статус контейнеров
docker-compose ps

# Проверить HTTPS
curl -I https://edunabazar.ru

# Проверить HTTP редирект
curl -I http://edunabazar.ru

# Проверить логи
docker-compose logs -f frontend
```

---

## ✅ Финальная проверка

### В браузере:

1. Открыть: `https://edunabazar.ru`
   - ✅ Зеленый замочек 🔒
   - ✅ Сертификат от Let's Encrypt
   - ✅ Сайт загружается

2. Открыть: `http://edunabazar.ru`
   - ✅ Автоматический редирект на HTTPS

3. Проверить SSL Labs:
   - https://www.ssllabs.com/ssltest/analyze.html?d=edunabazar.ru
   - ✅ Оценка: A или A+

---

## 📋 Что НЕ попадет в Git

### Автоматически игнорируется (.gitignore):

```
❌ /etc/letsencrypt/          # Сертификаты Let's Encrypt
❌ frontend/nginx-ssl.conf    # Конфигурация с реальным доменом
❌ .env                        # Секреты и пароли
❌ .env.backup                 # Backup .env
❌ *.key, *.crt, *.pem        # Любые сертификаты и ключи
```

### Что БУДЕТ в Git:

```
✅ SSL_*.md                    # Документация
✅ setup_ssl_prod.sh           # Production скрипт
✅ docker-compose.ssl.yml      # Docker конфигурация
✅ frontend/Dockerfile.ssl     # Dockerfile с SSL
✅ frontend/nginx-ssl.conf.template  # Шаблон конфигурации
✅ .gitignore                  # Обновленный
✅ README.md                   # Обновленный
```

---

## 🔄 Автообновление сертификатов

### Настроено автоматически:

- **Cron задача:** проверка каждый день в 3:00
- **Команда:** `certbot renew --quiet`
- **Действие:** автоматическое обновление за 30 дней до истечения
- **Перезапуск:** `docker-compose restart frontend`
- **Логи:** `/var/log/certbot-renew.log`

### Проверка автообновления:

```bash
# Посмотреть cron задачи
crontab -l

# Проверить срок действия сертификата
certbot certificates

# Тестовое обновление
certbot renew --dry-run
```

---

## 🐛 Troubleshooting

### Проблема: DNS не распространился

```bash
# Проверить DNS
dig edunabazar.ru
nslookup edunabazar.ru

# Если не работает - подождать еще
# DNS может занять до 48 часов
```

**Решение:** Подождать и запустить скрипт снова

---

### Проблема: Certbot не может получить сертификат

**Ошибка:** "Connection refused" или "DNS problem"

**Решение:**
```bash
# Проверить что порт 80 свободен
netstat -tulpn | grep :80

# Остановить контейнеры
docker-compose down

# Попробовать снова
bash setup_ssl_prod.sh
```

---

### Проблема: Nginx не запускается

**Решение:**
```bash
# Проверить логи
docker-compose logs frontend

# Проверить конфигурацию
docker-compose exec frontend nginx -t

# Проверить сертификаты
ls -la /etc/letsencrypt/live/edunabazar.ru/
```

---

## 📊 Структура файлов после настройки

### На сервере (production):

```
/opt/edu-na-bazar/
├── .env                           # ❌ НЕ в Git (секреты)
├── .env.backup                    # ❌ НЕ в Git (backup)
├── docker-compose.yml             # ✅ В Git (обновлен volumes)
├── frontend/
│   ├── nginx-ssl.conf            # ❌ НЕ в Git (реальный домен)
│   └── nginx-ssl.conf.template   # ✅ В Git (шаблон)
└── setup_ssl_prod.sh             # ✅ В Git

/etc/letsencrypt/
└── live/
    └── edunabazar.ru/
        ├── fullchain.pem         # ❌ НЕ в Git (сертификат)
        ├── privkey.pem           # ❌ НЕ в Git (ключ)
        └── chain.pem             # ❌ НЕ в Git (цепочка)
```

---

## 🎯 Итоговый чеклист

### Development (локально):

- [x] Создана документация (6 файлов)
- [x] Созданы скрипты (3 файла)
- [x] Обновлен .gitignore
- [x] Обновлен README.md
- [ ] **Закоммитить изменения**
- [ ] **Запушить в Git**

### DNS настройка:

- [ ] Добавить A-запись: @ → 176.99.5.211
- [ ] Добавить A-запись: www → 176.99.5.211
- [ ] Проверить DNS: `nslookup edunabazar.ru`
- [ ] Подождать 5-60 минут

### Production (сервер):

- [ ] Подключиться к серверу
- [ ] Обновить код: `git pull`
- [ ] Запустить: `bash setup_ssl_prod.sh`
- [ ] Проверить HTTPS: `https://edunabazar.ru`
- [ ] Проверить SSL Labs (оценка A/A+)

---

## 🎉 Результат

После выполнения всех шагов:

- ✅ Сайт работает по HTTPS
- ✅ Валидный сертификат Let's Encrypt
- ✅ Автоматический HTTP → HTTPS редирект
- ✅ Автообновление сертификатов каждые 90 дней
- ✅ Security headers настроены
- ✅ SSL Labs оценка A/A+
- ✅ Все секреты защищены (.gitignore)

**Ваш проект готов к production! 🚀🔒**
