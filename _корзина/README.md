# 🗑️ Корзина устаревших файлов

Эта папка содержит файлы, которые больше не используются в проекте, но сохранены для истории.

## 📂 Структура

### `mysql_миграция/`
**Устаревшие bat-файлы для миграции из MySQL**
- Все файлы связанные с MySQL 5.7, MariaDB, ENB
- Скрипты импорта/экспорта старой БД
- Файлы восстановления таблиц из .ibd файлов

**Причина устаревания:** Проект полностью перешел на PostgreSQL 15 + PostGIS. MySQL больше не используется.

### `старые_sql/`
**SQL дампы и инсерты из старой БД**
- Дампы MySQL базы данных (dump.sql, dump_clean.sql и т.д.)
- SQL инсерты для старых таблиц (advert_inserts.sql, user_inserts.sql и т.д.)
- Экспорты из ENB базы

**Причина устаревания:** Данные успешно мигрированы в PostgreSQL. Старые SQL файлы больше не нужны.

**Размер:** ~150 MB SQL файлов

### `старые_скрипты/`
**Python скрипты для извлечения данных из MySQL**
- extract_*.py - скрипты извлечения данных из старой БД
- migrate_*.py - скрипты миграции из MySQL в PostgreSQL
- analyze_*.py - анализ структуры MySQL таблиц

**Причина устаревания:** Миграция завершена. Скрипты использовались только один раз.

### `устаревшие_документы/`
**Документация по миграции и старым процессам**
- Инструкции по миграции (MIGRATION_*.md)
- Руководства по SSL (SSL_*.md)
- Документация по деплою на REG.RU
- Старые TODO и чеклисты
- Русскоязычные инструкции (НАЧАТЬ_ЗДЕСЬ.txt и т.д.)

**Причина устаревания:** 
- Миграция завершена
- SSL настроен и работает
- Процессы автоматизированы
- Актуальная документация в корне проекта (README.md, PROJECT_RULES.md)

### `дубликаты/`
**Тестовые и временные файлы**
- Тестовые файлы (test_image.jpg)
- Пустые лог-файлы

**Причина устаревания:** Тестовые файлы больше не нужны.

**Примечание:** Docker-compose файлы (dev, prod, ssl) были восстановлены обратно в корень проекта.

## ⚠️ Важно

**Эти файлы можно безопасно удалить**, но они сохранены на случай необходимости:
- Восстановления старых данных
- Анализа процесса миграции
- Справки по решенным проблемам

## 🧹 Рекомендации

1. **Через 3 месяца** - можно удалить папку `mysql_миграция/` и `старые_sql/`
2. **Через 6 месяцев** - можно удалить всю папку `_корзина/`
3. **Сейчас** - можно удалить `дубликаты/` (безопасно)

## 📊 Статистика

- **Перемещено файлов:** ~150
- **Освобождено места в корне:** Улучшена читаемость проекта
- **Размер корзины:** ~200 MB (в основном SQL дампы)

---

*Создано: 29.11.2025*
*Проект: Еду на базар*
