# 📱 Настройка SMS

## 🔧 Режим разработки (по умолчанию)

По умолчанию SMS **НЕ отправляются**. Коды просто выводятся в консоль backend.

### Как это работает:

1. Пользователь вводит номер телефона
2. Backend генерирует 6-значный код
3. **Код выводится в консоль** вместо отправки SMS
4. Пользователь вводит код из консоли
5. Авторизация успешна ✅

### Пример вывода в консоль:

```
============================================================
🔧 РЕЖИМ РАЗРАБОТКИ - SMS НЕ ОТПРАВЛЯЕТСЯ
============================================================
📱 Телефон: +79991234567
🔐 КОД ПОДТВЕРЖДЕНИЯ: 123456
⏰ Действителен: 5 минут
============================================================
```

### Настройка:

В файле `backend/.env`:
```env
SMS_ENABLED=false  # Режим разработки
```

---

## 🚀 Режим продакшена

Для реальной отправки SMS через SMSC.RU:

### 1. Создайте аккаунт на SMSC.RU

1. Зарегистрируйтесь на https://smsc.ru
2. Пополните баланс
3. Получите логин и пароль

### 2. Обновите credentials

В файле `backend/services/sms_service.py`:

```python
# SMSC.RU credentials
SMSC_LOGIN = "ваш_логин"
SMSC_PASSWORD = "ваш_пароль"
```

### 3. Включите отправку SMS

В файле `backend/.env`:
```env
SMS_ENABLED=true  # Режим продакшена
ENVIRONMENT=production
```

### 4. Перезапустите backend

```bash
cd backend
python main.py
```

---

## 📋 Конфигурация

### Параметры SMS (в `sms_service.py`):

```python
CODE_EXPIRE_MINUTES = 5  # Время жизни кода
CODE_LENGTH = 6           # Длина кода (6 цифр)
```

### Формат номера телефона:

Принимаются любые форматы:
- `+79991234567` ✅
- `89991234567` ✅
- `79991234567` ✅
- `9991234567` ✅

Все конвертируются в: `+79991234567`

---

## 🧪 Тестирование

### Режим разработки:

1. Запустите backend:
```bash
cd backend
python main.py
```

2. Откройте frontend:
```bash
cd frontend
npm start
```

3. Попробуйте войти:
   - Введите любой номер телефона
   - Посмотрите код в консоли backend
   - Введите код на frontend
   - ✅ Успешная авторизация!

### Режим продакшена:

1. Установите `SMS_ENABLED=true`
2. Проверьте баланс на SMSC.RU
3. Попробуйте войти с реальным номером
4. SMS придет на телефон

---

## 💰 Стоимость SMS (SMSC.RU)

- **Россия:** ~0.70 - 1.50 ₽ за SMS
- **Другие страны:** от 3 ₽ за SMS

**Рекомендация:** Пополните баланс на 100-500 ₽ для тестирования.

---

## 🔍 Логирование

### Режим разработки:
```
🔧 DEV MODE: SMS code for +79991234567: 123456
```

### Режим продакшена:
```
✅ SMS отправлено успешно! ID: 12345
📱 Номер: +79991234567
🔐 Код: 123456
🆔 SMS ID: 12345
⏰ Действителен 5 минут
```

### Ошибки:
```
❌ SMSC Error 1: Ошибка в параметрах
❌ Network error: Connection timeout
```

---

## 🛡️ Безопасность

### Рекомендации:

1. **Не коммитьте credentials в Git:**
   - Храните в `.env` (файл в `.gitignore`)
   - Используйте переменные окружения

2. **Ограничьте количество попыток:**
   - Максимум 3 попытки ввода кода
   - Блокировка на 1 час после 5 неудачных попыток

3. **Логируйте подозрительную активность:**
   - Множественные запросы с одного IP
   - Попытки перебора кодов

4. **На проде используйте HTTPS:**
   - Токены передаются только через HTTPS
   - Включите HSTS

---

## 📝 Примеры использования

### Python (backend):

```python
from services.sms_service import SMSService

# Отправить SMS
code = SMSService.generate_code()
success = await SMSService.send_sms("+79991234567", code)

# Проверить номер
is_valid = SMSService.validate_phone("+79991234567")

# Форматировать номер
formatted = SMSService.format_phone("89991234567")  # +79991234567
```

### TypeScript (frontend):

```typescript
// Отправить SMS
const response = await fetch('/api/auth/send-sms', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ phone: '+79991234567' })
});

// Проверить код
const response = await fetch('/api/auth/verify-sms', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ 
    phone: '+79991234567',
    code: '123456'
  })
});
```

---

## ❓ FAQ

**Q: Почему SMS не приходит?**
A: Проверьте:
- Баланс на SMSC.RU
- Правильность логина/пароля
- Формат номера телефона
- Логи в консоли backend

**Q: Можно ли использовать другой SMS провайдер?**
A: Да! Измените метод `send_sms()` в `sms_service.py`

**Q: Как отключить SMS на проде?**
A: Установите `SMS_ENABLED=false` в `.env`

**Q: Сколько стоит SMS?**
A: ~1 ₽ за SMS в России через SMSC.RU

---

## 🔗 Полезные ссылки

- [SMSC.RU API](https://smsc.ru/api/http/)
- [SMSC.RU Личный кабинет](https://smsc.ru/login/)
- [Документация FastAPI](https://fastapi.tiangolo.com/)
